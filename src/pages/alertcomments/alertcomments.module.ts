import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AlertcommentsPage } from './alertcomments';

@NgModule({
  declarations: [
    AlertcommentsPage,
  ],
  imports: [
    IonicPageModule.forChild(AlertcommentsPage),
  ],
})
export class AlertcommentsPageModule {}
